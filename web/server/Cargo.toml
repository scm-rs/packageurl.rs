[package]
name = "packageurl-web-server"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
axum = "0.8"
tower-http = { version = "0.6", features = ["fs"] }
tokio = { version = "1", features = ["full"] }
